#![allow(unused_imports)]

use std::collections::BTreeMap;

use std::fmt;
use std::fs::File;
use memmap::{Mmap};

mod bit_array;
mod dictionary;



fn main() -> Result<(), std::io::Error> {

  Ok(())
}
